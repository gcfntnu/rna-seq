#-*- mode:snakemake -*-
extra_conf_fn = srcdir('deeptools.config')
if os.path.exists(extra_conf_fn):
    with open(extra_conf_fn) as fh:
        c  = yaml.load(fh) or {}
        update_config2(config, c)

ORG = config.get('organism', 'homo_sapiens')

EFFECTIVE_GENOMESIZE = {''}
